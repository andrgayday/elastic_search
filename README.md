# 🚀 Book Search Service (Django + Elasticsearch)

Это проект поискового сервиса, реализованный на фреймворке Django с использованием Elasticsearch.

## 🛠️ Настройка и Запуск Проекта

Для запуска проекта необходимо выполнить следующую последовательность команд:

перейдите в директорию приложения **cd /elastic_search**

### 1. Подготовка Python Окружения

| Шаг | Команда | Описание |
| :--- | :--- | :--- |
| **1. Создание Виртуального Окружения** | `python -m venv .venv` | Создание изолированного Python-окружения. |
| **2. Активация Окружения** | `source .venv/bin/activate` | Активация окружения (для Linux/macOS). |
| **3. Установка Зависимостей** | `pip install -r requirements.txt` | Установка всех необходимых библиотек. |

### 2. Подготовка Базы Данных и Импорт Данных

| Шаг | Команда | Описание |
| :--- | :--- | :--- |
| **1. Применение Миграций** | `make migrate` | Применение миграций Django для создания таблиц. |
| **2. Создание Суперпользователя** | `python manage.py createsuperuser` | Запуск интерактивного процесса создания учетной записи администратора (потребуется ввод логина, почты, пароля). |
| **3. Импорт Книг** | `python manage.py import_books_to_db data_fixed.csv` | Запуск кастомной команды для загрузки данных из указанного CSV-файла в базу данных. |

### 3. Запуск Сервисов (Docker)

Запуск контейнера Elasticsearch.

| Шаг | Команда |
| :--- | :--- |
| **Сборка и Запуск** | `docker compose up --build` |

### 4. Настройка и Индексация Elasticsearch

После запуска контейнера Elasticsearch необходимо настроить поисковый индекс.

| Шаг | Команда | Описание |
| :--- | :--- | :--- |
| **1. Создание Индекса** | `python manage.py create_index index_name=book` | Создание индекса `book` в Elasticsearch. |
| **2. Проверка Индекса** | `python manage.py check_index` | Проверка статуса созданного индекса. |
| **3. Индексация Данных** | `python manage.py index_books` | Загрузка данных из базы данных Django в поисковый индекс Elasticsearch. |

### 5. Запуск Приложения и Доступ

| Шаг | Команда | Описание |
| :--- | :--- | :--- |
| **1. Запуск Сервера** | `python manage.py runserver` | Запуск Django-сервера разработки. |
| **2. Переход к Приложению** | **`localhost:8000`** | Откройте этот адрес в браузере для доступа к поисковому сервису. |